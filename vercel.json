{
  "buildCommand": "cd app && npm run build && cp -r dist ../dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
