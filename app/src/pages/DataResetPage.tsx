/**
 * DataResetPage.tsx
 * ❌ Removed from production — hard-wiping QMS data violates
 * 21 CFR Part 11, EU GMP Annex 11, and ALCOA+ data integrity.
 * If a dev-only reset is needed, use a CLI script instead.
 */
export function DataResetPage() {
  return null;
}
